apiVersion: v1
kind: Service
metadata:
  name: kubernetes-dashboard-lb
spec:
  type: LoadBalancer
  ports:
    - port: 8443
      targetPort: 8000
      protocol: TCP
      name: https
  selector:
    app.kubernetes.io/component: web
